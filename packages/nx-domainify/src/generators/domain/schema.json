{
  "$schema": "https://json-schema.org/schema",
  "$id": "Domain",
  "title": "Create a domain library",
  "description": "Creates an Angular domain library with application, entities, and infrastructure folders.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Domain name, for example booking. Do not include path segments; use --directory to nest the domain.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What domain name would you like to use?"
    },
    "directory": {
      "type": "string",
      "description": "Optional nested path under the workspace libs directory, for example sales. Creates libs/<directory>/<name>/domain."
    }
  },
  "required": ["name"]
}
