{
  "$schema": "https://json-schema.org/schema",
  "$id": "Domain",
  "title": "Create an Angular domain library.",
  "description": "Generates a domain library scoped to the domain.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the domain",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    }
  },
  "required": ["name"]
}
