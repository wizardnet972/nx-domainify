{
  "$schema": "https://json-schema.org/schema",
  "$id": "Domain",
  "title": "Nx Domainify Domain Generator",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the domain",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    }
  },
  "required": ["name"]
}
