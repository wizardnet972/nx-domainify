{
  "$schema": "https://json-schema.org/schema",
  "$id": "Init",
  "title": "Initialize nx-domainify",
  "description": "Adds DDD ESLint module-boundary constraints to the workspace.",
  "type": "object",
  "properties": {
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting files after the generator runs.",
      "default": false
    }
  }
}
