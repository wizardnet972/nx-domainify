{
  "$schema": "https://json-schema.org/schema",
  "$id": "Init",
  "title": "Nx Domainify Init Generator",
  "type": "object",
  "properties": {
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting the workspace after the generator runs"
    }
  }
}
