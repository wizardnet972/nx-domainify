{
  "$schema": "https://json-schema.org/schema",
  "$id": "Init",
  "title": "Initialize nx-domainify",
  "description": "Adds DDD ESLint module-boundary constraints and an AI skill that instructs agents to use nx-domainify generators.",
  "type": "object",
  "properties": {
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting files after the generator runs. Same as --skip-format.",
      "default": false,
      "aliases": ["skip-format"]
    },
    "dryRun": {
      "type": "boolean",
      "description": "Preview the changes without updating files. Same as --dry-run.",
      "default": false,
      "aliases": ["dry-run"]
    }
  }
}
