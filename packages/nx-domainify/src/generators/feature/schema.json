{
  "$schema": "https://json-schema.org/schema",
  "$id": "Feature",
  "title": "Create an Angular feature library.",
  "description": "Generates a feature library scoped to the domain.",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "Feature path including the feature name as the last segment (e.g. domain/feature-name).",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Provide the directory that includes the feature name."
    },
    "domain": {
      "type": "string",
      "aliases": ["domain", "domainName"],
      "description": "choose the domain project"
    }
  },
  "required": ["directory", "domain"]
}
